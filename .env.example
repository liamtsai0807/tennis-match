# 複製成 .env 再填。兩個 Supabase 的值都留白的話，App 會走內建示範資料
# （離線模式），流程一樣可以完整操作，只是資料只存在這台裝置。
#
# anon key 照設計就是公開的，真正的防線是資料庫的 RLS 政策。
# 會外洩出事的是 service_role key，那一個永遠不要放進前端。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# LINE LIFF app 的 ID。留白的話登入畫面不會顯示「用 LINE 登入」，
# Email 與 Google 兩條路不受影響。這個不是機密，本來就會出現在網址裡。
VITE_LINE_LIFF_ID=
